<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your OTP Code</title>
    <style>
        /* Modern Minimal Styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
        }
        
        body {
            background-color: #f8fafc;
            padding: 20px;
            color: #334155;
        }
        
        .email-wrapper {
            max-width: 500px;
            margin: 0 auto;
        }
        
        .card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }
        
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 32px 24px;
            text-align: center;
        }
        
        .logo {
            font-size: 24px;
            font-weight: 700;
            color: white;
            letter-spacing: -0.5px;
        }
        
        .content {
            padding: 40px 32px;
        }
        
        .greeting {
            font-size: 18px;
            color: #1e293b;
            margin-bottom: 16px;
            font-weight: 500;
        }
        
        .message {
            color: #64748b;
            line-height: 1.6;
            margin-bottom: 32px;
        }
        
        .otp-box {
            background: linear-gradient(135deg, #f0f4ff 0%, #f8fafc 100%);
            border: 1px solid #e2e8f0;
            border-radius: 10px;
            padding: 32px 24px;
            text-align: center;
            margin: 32px 0;
        }
        
        .otp-label {
            font-size: 14px;
            color: #64748b;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 12px;
            font-weight: 600;
        }
        
        .otp {
            font-size: 48px;
            font-weight: 700;
            color: #3b82f6;
            letter-spacing: 8px;
            font-family: 'Courier New', monospace;
            margin: 16px 0;
            line-height: 1;
        }
        
        .expiry {
            color: #64748b;
            font-size: 14px;
            margin-top: 16px;
        }
        
        .divider {
            height: 1px;
            background: #e2e8f0;
            margin: 32px 0;
        }
        
        .note {
            background: #fefce8;
            border-left: 4px solid #f59e0b;
            padding: 16px;
            border-radius: 6px;
            margin-top: 24px;
        }
        
        .note-title {
            color: #92400e;
            font-weight: 600;
            margin-bottom: 8px;
            font-size: 14px;
        }
        
        .note-text {
            color: #854d0e;
            font-size: 13px;
            line-height: 1.5;
        }
        
        .footer {
            text-align: center;
            padding: 24px;
            color: #94a3b8;
            font-size: 13px;
            border-top: 1px solid #e2e8f0;
        }
        
        @media (max-width: 480px) {
            .content {
                padding: 24px 20px;
            }
            
            .otp {
                font-size: 36px;
                letter-spacing: 6px;
            }
        }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <div class="card">
            <div class="header">
                <div class="logo">YourApp</div>
            </div>
            
            <div class="content">
                <h2 class="greeting">Hello <%= name %>,</h2>
                
                <p class="message">
                    Use the One-Time Password below to verify your identity. This OTP is valid for a single use.
                </p>
                
                <div class="otp-box">
                    <div class="otp-label">Your Verification Code</div>
                    <div class="otp"><%= otp %></div>
                    <div class="expiry">⏰ Expires in 10 minutes</div>
                </div>
                
                <div class="divider"></div>
                
                <p class="message" style="margin-bottom: 0;">
                    Enter this code on the verification page to complete your request.
                </p>
                
                <div class="note">
                    <div class="note-title">⚠️ Security Notice</div>
                    <p class="note-text">
                        Never share this OTP with anyone. Our team will never ask for your verification code.
                    </p>
                </div>
            </div>
            
            <div class="footer">
                <p>This is an automated message. Please do not reply to this email.</p>
                <p style="margin-top: 8px;">© <%= new Date().getFullYear() %> YourApp. All rights reserved.</p>
            </div>
        </div>
    </div>
</body>
</html>
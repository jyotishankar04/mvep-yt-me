<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Seller Notification' %></title>
    <style>
        /* Modern Minimal Styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
        }
        
        body {
            background-color: #f8fafc;
            padding: 20px;
            color: #334155;
        }
        
        .email-wrapper {
            max-width: 600px;
            margin: 0 auto;
        }
        
        .card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }
        
        .header {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            padding: 32px 24px;
            text-align: center;
        }
        
        .logo {
            font-size: 24px;
            font-weight: 700;
            color: white;
            letter-spacing: -0.5px;
            text-decoration: none;
        }
        
        .content {
            padding: 40px 32px;
        }
        
        .greeting {
            font-size: 20px;
            color: #1e293b;
            margin-bottom: 16px;
            font-weight: 600;
        }
        
        .message {
            color: #64748b;
            line-height: 1.6;
            margin-bottom: 24px;
        }
        
        .info-box {
            background: linear-gradient(135deg, #f0f9ff 0%, #f0fdf4 100%);
            border: 1px solid #dcfce7;
            border-radius: 10px;
            padding: 24px;
            margin: 24px 0;
        }
        
        .info-label {
            font-size: 14px;
            color: #047857;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            font-weight: 600;
        }
        
        .info-value {
            font-size: 24px;
            font-weight: 700;
            color: #059669;
            margin: 12px 0;
        }
        
        .details-table {
            width: 100%;
            border-collapse: collapse;
            margin: 24px 0;
            background: #f8fafc;
            border-radius: 8px;
            overflow: hidden;
        }
        
        .details-table th {
            background: #e2e8f0;
            padding: 12px 16px;
            text-align: left;
            font-weight: 600;
            color: #475569;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .details-table td {
            padding: 16px;
            border-bottom: 1px solid #e2e8f0;
            color: #475569;
        }
        
        .cta-button {
            display: inline-block;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            padding: 14px 32px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            margin: 24px 0;
            text-align: center;
            transition: transform 0.2s;
        }
        
        .cta-button:hover {
            transform: translateY(-2px);
        }
        
        .divider {
            height: 1px;
            background: #e2e8f0;
            margin: 32px 0;
        }
        
        .note {
            background: #fefce8;
            border-left: 4px solid #f59e0b;
            padding: 16px;
            border-radius: 6px;
            margin-top: 24px;
        }
        
        .note-title {
            color: #92400e;
            font-weight: 600;
            margin-bottom: 8px;
            font-size: 14px;
        }
        
        .note-text {
            color: #854d0e;
            font-size: 13px;
            line-height: 1.5;
        }
        
        .footer {
            text-align: center;
            padding: 24px;
            color: #94a3b8;
            font-size: 13px;
            border-top: 1px solid #e2e8f0;
        }
        
        @media (max-width: 480px) {
            .content {
                padding: 24px 20px;
            }
            
            .info-value {
                font-size: 20px;
            }
            
            .cta-button {
                width: 100%;
                padding: 16px 24px;
            }
        }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <div class="card">
            <div class="header">
                <a href="<%= appUrl || '#' %>" class="logo">
                    <%= appName || 'SellerPortal' %>
                </a>
            </div>
            
            <div class="content">
                <!-- Content will be injected here -->
                <%- body %>
            </div>
            
            <div class="footer">
                <p>This is an automated message from <%= appName || 'SellerPortal' %>.</p>
                <p style="margin-top: 8px;">© <%= new Date().getFullYear() %> <%= appName || 'SellerPortal' %>. All rights reserved.</p>
                <p style="margin-top: 8px; font-size: 12px; color: #cbd5e1;">
                    <a href="<%= unsubscribeUrl || '#' %>" style="color: #94a3b8;">Unsubscribe</a> | 
                    <a href="<%= helpUrl || '#' %>" style="color: #94a3b8;">Help Center</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>